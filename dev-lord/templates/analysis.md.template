# Source Code Analysis & Parity Catalog: [Source Application Name]

> **Purpose:** Authoritative inventory of the legacy/source application generated by **Road B (Step B3 & B5)**. Serves as the immutable functional baseline for migration into the target stack with zero regressions.

---

## 1. Source Environment & Tech Stack
- **Source Language & Runtime:** [e.g., Ruby 3.2, Python 3.10, PHP 8.2, Node 18]
- **Primary Framework & Version:** [e.g., Rails 7.0, Django 4.2, Laravel 10, Express 4.18]
- **Database Engine & ORM:** [e.g., PostgreSQL 14 with ActiveRecord / MySQL with Eloquent]
- **Legacy Test Suite Oracle:** [e.g., RSpec 3.12 (84 tests passing) | pytest (120 tests) | None detected]
- **Legacy Test Command:** [e.g., `bundle exec rspec` | `pytest` | N/A]
- **Pre-Flight Health Status:** [Complete / Partial with Missing Parts Noted]
- **Missing / Incomplete Components Noted:** [None or e.g., missing worker queue config, missing .env.example]

---

## 2. Core Data Entities & Schema Relationships
| Entity / Model | Database Table | Key Attributes & Types | Relations & Foreign Keys | Indexes / Uniques |
| :--- | :--- | :--- | :--- | :--- |
| `User` | `users` | `id (UUID)`, `email (String)`, `role (Enum)` | Has many `Orders` | Unique on `email` |
| `Order` | `orders` | `id (Int)`, `user_id (UUID)`, `total (Decimal)` | Belongs to `User` | Index on `user_id` |

---

## 3. Screen, View & Route Inventory
| Route / URL | View / Template File | User Role / Access | Key Functional UI Elements & Actions |
| :--- | :--- | :--- | :--- |
| `/dashboard` | `app/views/dashboard.html.erb` | Authenticated Users | KPI metric cards, recent transaction table, filter dropdown |
| `/orders/:id` | `app/views/orders/show.html.erb` | Buyer or Admin | Order items list, invoice download button, status timeline |

---

## 4. Hidden-Logic & Implicit Lifecycle Rules
*(Discovered via `<SKILL_DIR>/references/hidden-logic-patterns.md`)*

| Discovered Mechanism | Source File & Line | Trigger / Condition | Business Impact & Behavior |
| :--- | :--- | :--- | :--- |
| **Model Callback** | `app/models/order.rb:14` | `before_save :recalculate_tax` | Dynamically applies 8.25% state tax if shipping address is local. |
| **RLS Policy / Trigger** | `db/migrations/2023_rls.sql:22` | `auth.uid() = tenant_id` | Prevents cross-tenant query leaks at database engine level. |
| **Lifecycle Signal** | `signals.py:45` | `post_save` on `UserProfile` | Asynchronously invalidates Redis cache and triggers welcome webhook. |

---

## 5. Parity Baseline Catalog (Mandatory Verification Input/Output)
*(Direct feed to Road C Step C9 Migration Parity Check)*

### Sample Case 1: [Calculation / Permission / Transition Name]
- **Source Mechanism / Rule:** [e.g., `Order#calculate_discount` in `app/models/order.rb`]
- **Baseline Source:** [LIVE_OBSERVED (executed source app) | STATIC_INFERRED (deduced from code review)]
- **Representative Input Payload:**
  ```json
  {
    "order_id": 1042,
    "user_tier": "VIP",
    "subtotal": 250.00,
    "coupon_code": "SUMMER10"
  }
  ```
- **Observed / Inferred Source App Output & Behavior:**
  ```json
  {
    "discount_applied": 35.00,
    "final_total": 215.00,
    "tax": 17.74
  }
  ```
- **Verification Command / Test Fixture:** [e.g. `npm test -- order.test.js` or `curl -X POST ...`]
- **Parity Requirement:** Target implementation must yield identical output within 0.01 precision.

### Sample Case 2: [Permission / Authorization Edge Case]
- **Source Mechanism / Rule:** [e.g., Policy guard on `/api/v1/admin/users/:id`]
- **Baseline Source:** [LIVE_OBSERVED (executed source app) | STATIC_INFERRED (deduced from code review)]
- **Representative Input Payload:**
  ```json
  {
    "requester_role": "editor",
    "action": "delete_user",
    "target_user_id": 88
  }
  ```
- **Observed / Inferred Source App Output & Behavior:**
  ```json
  {
    "status": 403,
    "error": "Forbidden: Requires role [superadmin]"
  }
  ```
- **Verification Command / Test Fixture:** [e.g. `npm test -- auth.test.js` or `curl -X DELETE ...`]
- **Parity Requirement:** Target implementation must reject unauthorized non-superadmins with 403.
